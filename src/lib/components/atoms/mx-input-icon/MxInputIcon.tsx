import React, { ReactNode } from 'react';
import { Button, IButtonProps } from 'devextreme-react/text-box';

export const MxInputIcon: React.FC<IButtonProps> = (props) => {
  return <Button {...props}></Button>;
};
